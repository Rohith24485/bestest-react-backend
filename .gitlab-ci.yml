stages:
  - build

build_backend:
  stage: build
  image: maven:3.9.9-eclipse-temurin-17

  script:
    - chmod +x mvnw
    - ./mvnw clean package -DskipTests

  artifacts:
    paths:
      - target/*.jar

  only:
    - main